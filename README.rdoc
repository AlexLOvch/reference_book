== Reference book

Demo project implements in the admin area the reference books with dynamically managed fields.
It's possible to create new reference book, add/change/remove fields for reference book. Fields can be one of the following types: string, integer, float, date, datetime. 
Typecast will be done according to field's type.
All fields data edits with in place Ajax editor.

Used: ActiveAdmin, best_in_place.

